# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'EventSearch' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for EventSearch
  pod "McPicker"
  pod 'Alamofire', '~> 4.7'
  pod 'SwiftyJSON', '~> 4.0'
  pod "EasyToast"
  pod "SwiftSpinner"
  pod 'Kingfisher', '~> 4.0'
  pod 'GoogleMaps'
  pod 'GooglePlaces'
  target 'EventSearchTests' do
    inherit! :search_paths
    # Pods for testing
  end

end
